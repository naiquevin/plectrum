[package]
name = "simple"
version.workspace = true
edition.workspace = true

[dependencies]
plectrum = { version = "0.2.0", path = "../../plectrum", features = ["derive"] }
tokio = { version = "1.47.1", features = ["full"] }
